{
  "name": "@box-tracker/components",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "storybook dev -p 6005",
    "build": "build-storybook",
    "lint": "eslint --ext .ts,.tsx .",
    "format": "prettier --write .",
    "lint:styles": "stylelint \"**/*.{css,scss}\" --fix",
    "test": "test-storybook --coverage"
  }
}